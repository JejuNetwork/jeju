{
  "name": "{{APP_NAME}}",
  "version": "1.0.0",
  "description": "{{DESCRIPTION}}",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot api/worker.ts",
    "build": "bun build api/worker.ts --outdir=dist/worker --target=bun --minify",
    "start": "bun run dist/worker/worker.js",
    "deploy": "jeju publish",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.1.0",
    "@jejunetwork/config": "workspace:*",
    "elysia": "^1.1.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6",
    "typescript": "^5.7.3"
  }
}
